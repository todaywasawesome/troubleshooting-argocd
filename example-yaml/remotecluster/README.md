This is for creating a kind cluster with the name "remotecluster". It has the kubernetes API exposed on the host ip and not just localhost.

`kind create cluster --name remotecluster --config kind.yaml`

